name: "Debug hooks"
stages:
   after-upgrade-chroot:
     - commands:
        - echo > /after-upgrade-chroot
   after-reset-chroot:
     - commands:
        - echo > /after-reset-chroot
   after-install-chroot:
     - commands:
        - echo > /after-install-chroot
     - files:
       - content: |
           name: After install hook
           stages:
             after-install:
               - commands:
                 - echo > /run/elemental/workingtree/after-install
         path: /system/oem/after-install-hook.yaml
